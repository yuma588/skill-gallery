# GitHub Personal Access Token (required)
# Generate at: https://github.com/settings/tokens
# Required permissions: repo (for full functionality) or specific permissions
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Transport mode (optional): 'stdio' (default) or 'http'
TRANSPORT=stdio

# Port for HTTP server (optional, only used when TRANSPORT=http)
PORT=3000
